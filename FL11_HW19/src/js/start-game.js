/* const startGame = () => {
  let buttonValue = event.target.innerHTML;
  if (buttonValue !== 'Reset' && round <= 3) {
    playGame(buttonValue);
  } else if (round === 3) {
    let result;
    playerWins > computerWins ? (result = 'WON') : (result = 'LOST');
    gameResult.innerHTML = `You ${result}. Try again)`;
    endGame();
  } else {
    endGame();
  }
};

export { startGame }; */
